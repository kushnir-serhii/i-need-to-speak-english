# Tasks: Personal Dashboard (013-dashboard)

Generated by `/awos:tasks`. Each slice leaves the application in a runnable, working state.

---

- [x] **Slice 1: Rename `/api/owner/` routes to `/api/admin/`**
  - [x] Move the entire `src/app/api/owner/` directory tree to `src/app/api/admin/` (no logic changes — path rename only). **[Agent: nextjs-backend]**
  - [x] Update all client-side `fetch` call sites to use the new paths: `EnrollmentGate.tsx`, `src/app/(admin)/admin/page.tsx`, `src/app/login/page.tsx`, and any remaining `/api/owner/` references. **[Agent: nextjs-frontend]**
  - [x] Update `context/product/architecture.md` to reflect the new `/api/admin/` paths. **[Agent: general-purpose]**
  - [x] Verify: zero `/api/owner/` references remain in `src/`; `npx tsc --noEmit` exits with zero errors. **[Agent: nextjs-backend]**

---

- [x] **Slice 2: Dashboard shell + Usage Indicator**
  - [x] Create `src/components/dashboard/UsageIndicator.tsx` — reads `useUserStore` / `useChatStore` / `useSettingsStore` to render either `"X / Y messages today"` (shared-key) or `"X tokens used"` (BYO-key). **[Agent: nextjs-frontend]**
  - [x] Replace the stub content in `src/app/(admin)/dashboard/page.tsx` with a layout shell containing one `<section>` card for `<UsageIndicator />`. **[Agent: nextjs-frontend]**
  - [x] Verify: `/dashboard` loads for enrolled visitor; "0 / 20 messages today" visible. Fixed middleware that was incorrectly blocking `/dashboard` for non-admin visitors. **[Agent: nextjs-frontend]**

---

- [x] **Slice 3: Language Selector Card**
  - [x] Add `customPrompt: string` and `useCustomPrompt: boolean` fields (defaults `""` / `false`) to `useSettingsStore`. **[Agent: nextjs-frontend]**
  - [x] Create `src/components/dashboard/LanguageSelectorCard.tsx` — searchable language dropdown; on selection: POST current session to `/api/history`, call `setTargetLanguage`, `clearMessages()`, `initSessionId()`. **[Agent: nextjs-frontend]**
  - [x] Add `<LanguageSelectorCard />` as the second `<section>` card in `dashboard/page.tsx`. **[Agent: nextjs-frontend]**
  - [x] Verify: language selector visible; selecting Spanish cleared chat and persisted across page reload. **[Agent: nextjs-frontend]**

---

- [x] **Slice 4: Prompt Management Card (UI + store persistence)**
  - [x] Create `src/components/dashboard/PromptCard.tsx` — Default/Custom pill toggle + textarea; wires to `setCustomPrompt` / `setUseCustomPrompt`; textarea is dimmed when toggle is "Default". **[Agent: nextjs-frontend]**
  - [x] Add `<PromptCard />` as the third `<section>` card in `dashboard/page.tsx`. **[Agent: nextjs-frontend]**
  - [x] Verify: text preserved after toggle to Default; text and toggle state survive full page reload. **[Agent: nextjs-frontend]**

---

- [x] **Slice 5: Wire custom prompt to chat API (end-to-end activation)**
  - [x] Update `buildSystemPrompt` in `src/lib/systemPrompt.ts` to accept an optional `customPrompt?: string` second parameter — returns it as-is when non-empty and trimmed, otherwise falls back to the default template. **[Agent: nextjs-backend]**
  - [x] Update `POST /api/chat/route.ts` to accept `customPrompt` (string, max 2 000 chars) and `useCustomPrompt` (boolean) from the request body; apply the conjunctive selection logic before the OpenAI call. **[Agent: nextjs-backend]**
  - [x] Update the chat-send logic in `src/components/chat/ChatInput.tsx` to pass `customPrompt` and `useCustomPrompt` from `useSettingsStore` in each chat request body. **[Agent: nextjs-frontend]**
  - [x] Verify: Custom mode → AI replied "CUSTOM PROMPT ACTIVE" verbatim; Default mode → normal Spanish greeting. End-to-end confirmed. **[Agent: nextjs-frontend]**
